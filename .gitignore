# Python
__pycache__/
*.pyc
.venv/
venv/

# Fichiers de travail temporaires
*.tmp
sim_sample.json
brgm_caps.xml

# La page générée est déployée par le workflow, pas versionnée en clair.
# (On garde template.html ; index.html est régénéré puis chiffré.)
site/index.html
# Fichiers statiques régénérés par le robot (b) : géométrie + historique.
site/geom_*.json
site/hist_*.json

# Sortie de chiffrement StatiCrypt + config auto-générée
public/
_public_test/
.staticrypt.json

# macOS
.DS_Store
_versant_cache/
# Prototypes locaux (exploration versant) — non déployés, un fichier fait 38 Mo.
prototypes/

# Corbeille locale (deplacements, pas de rm depuis le pont) + script de push local
_to_delete/
git-push.sh
